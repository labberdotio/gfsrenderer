query ipxedhcp2 {
  IPXEDHCPServices(
      name: "ipxedhcp2"
    ) {
    name,
    description,
    defaultLeaseTime,
    maxLeaseTime,
    deployed {
      name,
      address
    }
    subnets {
      name, 
      description, 
      network {
        name,
        address
      },
      netmask {
        name,
        address
      },
      rangeStart {
        name,
        address
      },
      rangeEnd {
        name,
        address
      },
      routes {
        name,
        address
      },
      routers {
        name,
        address
      },
      resolvers {
        name,
        address
      }
      # ,
      # local {
      #   name,
      #   address
      # }
    },
    resolvers {
      name,
      description,
      members {
        name,
        description,
        addresses {
          address
        }
      }
    },
    hosts {
      name,
      description,
      members {
        name,
        description,
        interfaces {
          name,
          hwaddr,
          addresses {
            id,
            name,
            address
          }
        },
        interface {
          name,
          hwaddr,
          addresses {
            name,
            address
          }
        }
      }
    },
    aliases {
      name,
      description,
      members {
        name,
        host {
          name,
          description,
          interfaces {
            name,
            hwaddr,
            addresses {
              name,
              address
            }
          },
          interface {
            name,
            hwaddr,
            addresses {
              name,
              address
            }
          }
        }
      }
    },
    bootDirectives {
      name,
      description,
      members {
        name,
        description,
        machine {
          name,
          description,
          defaultInterface {
            name,
            device,
            hwaddr, 
            addresses {
              address
            }
          }
          managementInterface {
            name,
            device,
            hwaddr, 
            addresses {
              address
            }
          }
        },
        nodeBoot,
        distro {
          name,
          description,
          version,
          image,
          port,
          uri,
          kernel,
          initrd,
          imgargs
        },
        kickstart {
          name,
          description,
          path,
          port,
          uri
        }
        iSCSIServerHost,
        iSCSIServerPort,
        iSCSILun,
        nodeInstallInterface,
        localInstallIPXEDevice,
        distroInstallDevice
      }
    },
    kickStarts {
      name,
      description,
      members {
        name,
        description,
        port,
        path,
        uri,
      }
    },
    installImages {
      name,
      description,
      members {
        name,
        description,
        version,
        image,
        port,
        uri,
        kernel,
        initrd,
        imgargs,
      }
    }
  }
}
