query installs2 {
  InstallImageGroups(
      name: "installs2"
    ) {
    name,
    description,
    members {
      name,
      description,
      version,
      image,
      port,
      uri,
      kernel,
      initrd,
      imgargs
    }
  }
}
